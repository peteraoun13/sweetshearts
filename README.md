# Sweetshearts Cake Studio

A modern React and Vite website for a premium custom cake studio.

## Run Locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The current logo is a text fallback because no logo file was present in the workspace. Add the final logo path in `src/data/brand.js` when it is available.
